# Pin pnpm's default explicitly. Without this, a contributor whose global
# config differs bakes their value into pnpm-lock.yaml, and every frozen
# install elsewhere fails with ERR_PNPM_LOCKFILE_CONFIG_MISMATCH.
auto-install-peers=true
